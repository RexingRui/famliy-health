# 家庭健康管理 · 设计稿要点

> 原稿：[家庭健康管理 · 设计稿](https://claude.ai/code/artifact/181d5e62-5cf3-4c2e-b088-7cdd0203ca67)（设计画布，可交互）。本文件只摘录开发需要的规范，像素细节以原稿为准。

## 画板清单

| 画板 | 端 | 尺寸 | 对应路由 | 要点 |
|---|---|---|---|---|
| 首页 | 手机 | 390×844 | `/` | 顶部日期 + 标题“家里的病程”，右上“待整理 N”；成员头像横排（头像下显示“感冒第 2 天”等状态，末尾“添加”）；进行中的病程卡片（短期：大号“第 N 天”+ 体温迷你曲线 + 上次用药；长期：状态标签 + 本周 7 天小日历 + 本周摘要）；底部标签栏 |
| 记一笔 | 手机 | 390×844 | `/record/new` | 顶部“取消 / 记一笔”；“给谁记”成员胶囊；内容卡片（发生时间、文字框、语音条 +“给语音补一句文字”、照片缩略图）；“按住说话”大按钮 + 拍照 + 相册；类型选填（横排 7 个）；归入病程行（“更换”）；底部深色“保存” |
| 记一笔：录音中，类型选用药 | 手机 | 390×844 | `/record/new` | 选中类型高亮；用药字段（药名、剂量、单位 ml/片/粒/袋）+ 蓝色“上次服用”提示；底部白色弹层：正在录音、计时、波形、88px 麦克风按钮、“松开保存，上滑取消” |
| 病程详情：短期（感冒） | 手机 | 390×1200 | `/episodes/:id` | 成员与年龄、病程名（展示字体 38px）+ 状态标签、概况一行；“切换状态 / 导出报告”；分段控件“时间线 / 日历 / 趋势曲线”；按天分组的时间线（左侧时间，右侧卡片，含“补录”标记与“再记一次”）；底部“在这个病程里记一笔” |
| 病程详情：长期（日历视图） | 手机 | 390×1080 | `/episodes/:id?view=calendar` | 月历，每格用图形标记记录类型，下方图例；点某天显示当天记录 |
| 添加成员 | 手机 | 390×1020 | `/members/new` | 头像（选填）、称呼、关系胶囊（本人/配偶/子女/父母/祖辈/其他）、性别、出生日期（提示“用来计算年龄”）；“健康信息（都可以以后再补）”：过敏史、血型（A/B/AB/O/不清楚）、备注；“保存成员” |
| 电脑端：家庭总览 | 电脑 | 1440×900 | `/` | 左侧 240px 侧栏；主区标题 + 搜索框“搜索记录、药名、医院”；每个成员一栏（头像、性别年龄、过敏、当前病程卡片、最近记录列表、“近一年 N 个病程 · 查看全部”） |
| 电脑端：成员主页（按病种） | 电脑 | 1440×900 | `/members/:id` | 成员信息（性别年龄生日、过敏、血型）+“导出健康档案”；视图“时间线 / 日历 / 按病种”；时间段“近一年 / 近三年 / 全部”；病种卡片（次数）；选中病种的摘要、按月分布、“每一次的经过”表格 |
| 电脑端：待整理 | 电脑 | 1440×900 | `/inbox` | 左列表：可勾选，“已选 N 条 · 一起归入 [病程] 归入 / 删除”；右侧“编辑记录”面板：成员、发生时间、类型、程度 1–10、语音（再录一段）、文字、照片、归入病程（含暂不归类、新建病程）、保存 / 取消 |
| 电脑端：导出报告 | 电脑 | 1440×900 | `/export` | 左侧设置：报告类型（病程报告 / 成员健康档案）、选择病程、包含内容勾选、照片（不附带 / 附缩略图 / 作为附录附原图）、“下载 PDF / 打印”；右侧 A4 预览（页脚“家庭自行记录，仅供就诊时参考，不作为诊断依据”） |

设计稿未覆盖、需按设计语言补的页面：登录页、记录详情与编辑页、手机端成员列表。

## 视觉规范

Tailwind 主题变量在 `web/src/styles/index.css`，命名与下表一致（如 `bg-page`、`text-ink-muted`、`bg-medication-soft`）。

### 颜色

| 变量 | 值 | 用途 |
|---|---|---|
| page | #EDF1EF | 页面背景 |
| surface | #FFFFFF | 卡片、侧栏、标签栏 |
| field | #FAFBFA | 输入框背景 |
| segment | #DDE3E1 | 分段控件底色 |
| ink | #1B2826 | 正文、深色按钮（保存） |
| ink-muted | #4E5D5A | 次要文字 |
| ink-subtle | #8A9693 | 占位、虚线边框 |
| line | #D5DCD9 | 边框、分隔线 |
| line-soft | #EEF1F0 | 列表内细分隔 |
| primary / primary-soft | #1E6B58 / #DCEBE5 | 主色：记一笔、选中态、链接 |
| alert / alert-soft | #B04E15 / #F6E4D8 | 待整理数量徽标、“第 N 天”、进行中 / 治疗中状态 |

记录类型配色（前景 / 浅底），标签、日历、曲线统一使用：

| 类型 | 前景 | 浅底 | 日历标记 |
|---|---|---|---|
| 症状 | #B04E15 | #F6E4D8 | 橙色实心圆，数字为当天最高程度 |
| 体温 | #8C560A | #F7EBD3 | —（看趋势曲线） |
| 用药 | #2C679E | #DDE8F3 | 蓝色竖条 |
| 治疗康复 | #1E6B58 | #DCEBE5 | 绿色圆环 |
| 就诊 | #1B2826 | #E3E7E6 | 黑色方块 |
| 检查 / 其他 | #4E5D5A | #ECEFEE | — |

### 字体

- 正文：`'Noto Sans SC', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif`
- 展示（页面大标题、病程名、成员头像字）：`'ZCOOL XiaoWei', 'Songti SC', 'STSong', serif`
- 设计稿通过 Google Fonts 加载字体；国内访问不稳定，项目内**不引用 Google Fonts**，先用系统字体回退。如需 ZCOOL XiaoWei，按用到的字做子集后放到本站静态资源中自托管。

### 尺寸与形状

- 触控目标不小于 44px；主按钮高 52px（保存）或 60px（按住说话）。
- 圆角：卡片 18px，控件 12px，胶囊按钮 22px，时间线卡片 14px。
- 手机页面左右边距 16px（标题区 20px）；电脑端侧栏 240px，主区内边距 32px 40px。
- 响应式分界 1024px（Tailwind `lg`），以下为手机外壳，以上为电脑外壳。
- 手机底部标签栏高 84px；中间“记一笔”为 64px 圆形按钮，上浮 30px。

### 图标

线性描边图标（stroke 1.8–2，圆角端点），不使用 emoji。已有图标在 `web/src/components/icons.tsx`。
