import { PagePlaceholder } from '../../components/PagePlaceholder'

export function RecordNewPage() {
  return (
    <PagePlaceholder
      title="记一笔"
      summary="选成员、文字框、按住录音、拍照或选图；类型与附加字段收在“更多”里；按规则默认归入病程。最短路径：记一笔 → 录音 → 保存。电脑端以弹窗形式打开。" design="记一笔、记一笔：录音中，类型选用药"
    />
  )
}
