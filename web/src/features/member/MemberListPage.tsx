import { PagePlaceholder } from '../../components/PagePlaceholder'

export function MemberListPage() {
  return (
    <PagePlaceholder
      title="成员"
      summary="手机端的成员列表就是首页的成员头像栏，不单独设计；电脑端成员列在侧栏。此路由跳转到第一个成员的主页。"
    />
  )
}
